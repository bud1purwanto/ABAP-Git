fastapi>=0.111.0
uvicorn[standard]>=0.30.1
sqlalchemy>=2.0.30
psycopg[binary]>=3.2.10
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0.1
bcrypt>=4.2.0
anthropic>=0.28.0
google-generativeai>=0.5.4

# pyrfc is NOT installed via pip — it requires the SAP NetWeaver RFC SDK
# to be installed on this machine first, then: pip install pyrfc
# See SAP Note 2573790. Install it manually once the SDK is in place.
